<!DOCTYPE html>
<html>

<head>
    <title>
        <%=title%>
    </title>

    <!--<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.js"></script>
-->

    <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.1/noty.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.1/noty.min.js"></script>


    <link rel="stylesheet" href="/css/layout.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/footer.css">

    <%- style %>

        <script>
            //this show on client side  with fetch req and res
            function shoWNotification(message, success) {
                const type = success ? 'success' : 'error';
                new Noty({
                    theme: 'relax',
                    text: message,
                    type: type,
                    layout: 'top',
                    timeout: 1500
                }).show();
            }
        </script>
</head>

<body>

    <!-- <header>Page Header </header>-->
    <%- include('_header'); %>


        <!--<if(flash.success && flash.success.length>0){>
                <h5>
                    <=flash.success>
                </h5>
                <}>-->

        <main id="layout-main">
            <%- body %>
        </main>

        <%- include('_footer'); %>
            <!-- <footer>Page Footer</footer>-->

            <%- script %>

                <script>
                    <% if(flash.success && flash.success.length>0){%>

                    new Noty({
                        theme: 'relax',
                        text: "<%=flash.success%>",
                        type: 'success',
                        layout: 'top',
                        timeout: 1500
                    }).show();
                    <%}%>

                    <% if(flash.error && flash.error.length>0){%>

                    new Noty({
                        theme: 'relax',
                        text: "<%=flash.error%>",
                        type: 'error',
                        layout: 'top',
                        timeout: 1500
                    }).show();
                    <%}%>
                </script>

</body>

</html>